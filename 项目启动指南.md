# 新闻项目启动指南

## 项目结构

- **frontend-vue/** - 前端Vue项目
- **src/** - 后端Java Spring Boot项目
- **database.sql** - 数据库初始化脚本

## 前置条件

### 1. 安装 Node.js

请参考 `Node.js安装指南.md` 文件，下载并安装 Node.js LTS 版本。

### 2. 安装 Java

系统已安装 Java 1.8.0_202，满足后端项目需求。

### 3. 安装 MySQL

需要安装 MySQL 数据库，并创建相应的数据库和表。

## 启动步骤

### 步骤 1: 配置数据库

1. 打开 MySQL 客户端（如 MySQL Workbench 或命令行）
2. 执行 `database.sql` 文件中的 SQL 语句：
   - 创建数据库 `springboot_demo`
   - 创建 `user` 表

3. **注意**：后端配置文件 `application.yml` 中使用的数据库名称是 `newsdb`，而 SQL 文件创建的是 `springboot_demo`。需要修改其中一个保持一致：
   - 方案 A: 修改 `application.yml` 中的数据库名称为 `springboot_demo`
   - 方案 B: 修改 `database.sql` 中的数据库名称为 `newsdb`

### 步骤 2: 安装前端依赖

1. 打开命令提示符或 PowerShell，进入前端项目目录：
   ```
   cd frontend-vue
   ```

2. 安装依赖：
   ```
   npm install
   ```

### 步骤 3: 启动前端开发服务器

在前端项目目录下运行：
```
npm run dev
```

前端服务器会在 `http://localhost:5173` 启动。

### 步骤 4: 启动后端服务器

1. 打开命令提示符或 PowerShell，进入项目根目录

2. 使用 Maven 启动 Spring Boot 应用：
   ```
   mvn spring-boot:run
   ```

   或使用已编译的 JAR 文件：
   ```
   java -jar target/news-0.0.1-SNAPSHOT.jar
   ```

后端服务器会在 `http://localhost:8080` 启动。

## 项目访问

1. 前端页面：`http://localhost:5173`
2. 后端 API：`http://localhost:8080/api`

## 注意事项

1. 确保 MySQL 服务正在运行
2. 确保数据库配置正确（数据库名称、用户名、密码）
3. 前端和后端服务器需要同时运行
4. 如果遇到跨域问题，后端已经配置了 CORS 支持

## 技术栈

- **前端**：Vue 3 + Vue Router + Axios + Pinia
- **后端**：Java Spring Boot + MyBatis-Plus
- **数据库**：MySQL
- **构建工具**：Vite（前端）、Maven（后端）